import 'package:dartz/dartz.dart';

import '../../core/error/failures.dart';
import '../entities/auth.dart';

abstract class AuthRepository {
  // Future<Either<Failure, Auth>> loginOperation(String userName, String password);
}
